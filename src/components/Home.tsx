export const Home = () => {
  return (
    <div
      id="home"
      className="page-view bg-slate-400"
    ></div>
  );
};

{
  /* <video
            className="pointer-events-none"
            autoPlay
            muted
            loop
            playsInline={true}
            key={rotorVideo}
          >
            <source src={rotorVideo} type="video/mp4" />
          </video> */
}
